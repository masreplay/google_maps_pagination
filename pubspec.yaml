name: google_maps_pagination
description: Pagination with pageView and markers throw google map
version: 0.0.2
homepage: http://www.matheer.com
repository: https://github.com/masreplay/google_maps_pagination

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.1.10
  flutter_map: ^4.0.0
  latlong2: ^0.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
